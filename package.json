{
  "name": "cap-codex",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^9",
    "express": "^4"
  },
  "engines": {
    "node": ">=22.14.0"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^0.4.0",
    "@cap-js/cds-types": "^0.13.0",
    "@cap-js/sqlite": "^2",
    "@types/express": "^4",
    "@types/node": "^22",
    "ts-node": "^10.9.2",
    "typescript": "^5",
    "cds-plugin-ui5": "^0.13.4",
    "eslint": "^8",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "build": "tsc",
    "start": "npm run build && cds-serve",
    "dev": "cds watch",
    "lint": "eslint .",
    "test": "npm run build && node --test test/*.test.js"
  },
  "sapux": [
    "app/books"
  ]
}
